#include <stdio.h>
#include <stdlib.h>

int main(void) {
  
  int *matA, *matB, *matC, m, n, p, contI, contJ;

  printf("Digite o tamanho M da matriz: \n");
  scanf("%d", &m);
  printf("Digite o tamanho N da matriz: \n");
  scanf("%d", &n);
  printf("Digite o tamanho P da matriz: \n");
  scanf("%d", &p);

  
  
  
  free(matA);
  free(matB);
  free(matC);

  return 0;
}